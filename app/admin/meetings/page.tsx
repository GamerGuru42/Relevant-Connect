import { MeetingManager } from '@/components/admin/MeetingManager'

export const metadata = {
  title: 'Meeting Manager | Admin | Relevant+',
}

export default function AdminMeetingsRoute() {
  return <MeetingManager />
}

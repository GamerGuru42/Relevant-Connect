import { BibleReader } from '@/components/bible/BibleReader'

export const metadata = {
  title: 'Bible - Relevant+',
  description: 'Read the Bible in multiple translations. Available offline.',
}

export default function BiblePage() {
  return <BibleReader />
}

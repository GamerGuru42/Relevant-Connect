import { MeetingsPage } from '@/components/meetings/MeetingsPage'

export const metadata = {
  title: 'Meetings - Relevant+',
  description: 'Join virtual meetings and fellowships',
}

export default function MeetingsRoute() {
  return <MeetingsPage />
}

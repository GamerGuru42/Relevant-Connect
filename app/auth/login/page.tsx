import { LoginForm } from '@/components/auth/LoginForm'

export const metadata = {
  title: 'Login - Relevant+',
  description: 'Sign in to your Relevant+ account',
}

export default function LoginPage() {
  return <LoginForm />
}
